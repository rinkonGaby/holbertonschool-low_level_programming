#include "main.h"

/**
 * main - assigns a random number to int n everytime
 * it executes, and prints it
 * Return: Always 0 (Success)
 */

int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
