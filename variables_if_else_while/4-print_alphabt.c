#include <stdio.h>

/**
 * main - prints the alphabet,
 * followed by a new line
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar(ch);
	}
	if(c != 'q' && c != 'e' )
	{
		putchar (c) ;
	}
	putchar('\n');
	return (0);
}
